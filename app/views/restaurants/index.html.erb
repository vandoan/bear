<%- model_class = Restaurant -%>

<div class="page-header">
  <h1 align= "center">Grea...   eats<%= image_tag "pig.png", :width => 110, :class => "corner"%> <h1>
</div>
<table class="table table-striped">
  <thead>
    <tr>
      <th><%= model_class.human_attribute_name(:image) %></th>
     <th><%= model_class.human_attribute_name(:name) %></th>
      <th><%= model_class.human_attribute_name(:location) %></th>
      <th><%= model_class.human_attribute_name(:about) %></th>
      <th><%= model_class.human_attribute_name(:number) %></th>
      <th><%= model_class.human_attribute_name(:created_on) %></th>
      <th><%= model_class.human_attribute_name(:menu) %></th>
     </tr>

  </thead>
  <tbody>


    <% @restaurants.each do |restaurant| %>
      <tr>

        <td><%= link_to(image_tag(restaurant.image_url, :width => 100, :class => "cornerSm"), restaurant_path(restaurant)) if restaurant.image %></td>
         <td><%= restaurant.name %></td>
        <td><%= restaurant.address %></td>
        <td><%= restaurant.description %></td>
        <td><%= restaurant.phone %></td>
        <td><%= restaurant.created_at.strftime("%a %b %y")  %></td>
        <td><%= image_tag(restaurant.menu_url, :width => 100, :class => "cornerSm") if restaurant.menu.present? %></td>
         <td>      
 

 
         </td>
      </tr>
<% end %> 

  </tbody>
</table>
<% if owner_signed_in? %> 

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_restaurant_path,
            :class => 'btn btn-primary' %>
<% end %> 
  <br /> 
  <h4> Find us in New York </h4> 
    <div class="iframe"> 
      <div class="iframe"> 
      <iframe  src="https://www.google.com/maps/embed/v1/place?q=Financial%20District%2C%20NY%2C%20United%20States&key=AIzaSyCiEPXZzGR6FL0IqoWaVDiwL10lpYp4cyo", ></iframe>
    </div> 

        <div id="map" style="position: relative; background-color: rgb(229, 227, 223); overflow: hidden;">

          <div class="gm-style" style="position: absolute; left: 0px; top: 0px; overflow: hidden; width: 100%; height: 100%; z-index: 0;">
        </div>
  </div> 
</div> 


<div id="map1" class="gmap left bling">
</div>





